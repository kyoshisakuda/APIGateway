spring:
  application:
    name: apigateway

server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8181/eureka

zuul:
  ignoredServices: '*'
  routes:
    auth-service:
      path: /auth/**
      service-id: auth-service
      stripPrefix: false
      sensitiveHeaders: Cookie,Set-Cookie
#    accounting-service:
#      path: /accounting-service/**
#      service-id: accounting-service